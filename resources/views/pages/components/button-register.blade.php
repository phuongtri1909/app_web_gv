<div class="wrap-button" title="{{ $buttonTitle ?? 'Đăng ký ngay' }}" style="{{ $buttonPosition ?? 'right:15px' }}">
    <a href="{{ $buttonLink ?? '#' }}" title="{{ $buttonTitle ?? 'Đăng ký ngay' }}" class="button-register">
        {{ $buttonTitle ?? 'Đăng ký ngay' }}
    </a>
</div>